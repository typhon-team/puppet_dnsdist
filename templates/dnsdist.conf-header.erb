webserver("<%= @webserver %>", "<%= @webserver_pass %>")
controlSocket("<%= @control_socket %>")
setServerPolicy(<%= @server_policy %>)
<% @listen_addresses.each do |listen_address| 
  -%>addLocal("<%= listen_address %>")
<% end -%>
